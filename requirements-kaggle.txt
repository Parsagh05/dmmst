# Verified working set (Python 3.11). Four pins below are non-obvious --
# each one was an actual failure before being pinned.

# NOTE on Kaggle's "GPU T4 x2": two visible GPUs make HF Trainer use DataParallel,
# which crashes this model with "chunk expects at least a 1-dimensional tensor".
# Run with CUDA_VISIBLE_DEVICES=0 (the notebooks do this automatically).

# --- torch: install FIRST, from the CUDA index, or pip pulls a CPU/newer build ---
# pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#     --index-url https://download.pytorch.org/whl/cu121
# On Kaggle, torch is preinstalled with a matching CUDA build -- skip it there
# and check `torchsurv` compatibility (see pin below).

transformers==4.50.0          # repo subclasses HF internals; 4.57 breaks modeling_bert.py
datasets==3.4.1
tokenizers>=0.21,<0.22
numpy<2.2                     # required by numba 0.61
accelerate>=1.4.0
evaluate>=0.4.3

hydra-core>=1.3.2
hydra-colorlog>=1.2.0
omegaconf

# PIN 1: torchsurv 0.2.x requires torch>=2.8. Provides IPCW C-index + Brier.
torchsurv==0.1.4
# PIN 2: lifelines 0.27.8 imports scipy.integrate.trapz, removed in scipy 1.14.
lifelines==0.27.8
scipy<1.14
# PIN 3: bootstrap-stat 0.3.x removed the bootstrap_stat submodule that
#        sat/utils/statistics.py imports.
bootstrap-stat==0.2.4.2
# PIN 4: bootstrap-stat pulls pathos, which wants dill>=0.4.1, but datasets
#        3.4.1 requires dill<0.3.9. pathos 0.3.2 is compatible with both.
pathos==0.3.2
dill<0.3.9
multiprocess<0.70.17

pandas>=2.2.3
scikit-learn>=1.6.1
h5py>=3.13
tables>=3.9.2
polars>=0.20.15,<0.21
pyarrow>=15,<16
fastparquet
logdecorator>=2.5
einops==0.8.1
numba==0.61.0
torchtuples>=0.2.2
seaborn>=0.13.2
plotly>=6.0.1
glom>=24.11
joblib>=1.4.2
tensorboard>=2.19
nvidia-ml-py                  # NOT nvidia-ml-py3: that wheel is from 2017 and cannot locate nvml
tqdm                          # progress bars; already a transformers/datasets dep, pinned here because the notebooks drive it directly
