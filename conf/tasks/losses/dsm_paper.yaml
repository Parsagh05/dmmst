# DSM conditional Weibull loss, faithful to Nagpal et al. / auton-survival.
survival:
  _target_: sat.loss.DSMPaperLoss
  duration_cuts: ${data.label_transform.save_dir}/duration_cuts.csv
  importance_sample_weights: ${data.label_transform.save_dir}/imp_sample.csv
  num_events: ${data.num_events}
  discount: ${dsm_paper_discount}
  elbo: ${dsm_paper_elbo}
